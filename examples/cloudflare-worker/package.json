{
  "name": "cleat-webhook-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Receive and verify a Cleat webhook on Cloudflare Workers with WebCrypto",
  "license": "MIT",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
